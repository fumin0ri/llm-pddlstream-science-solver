# Task
You are a PDDL expert. Your task is to correct all errors and flaws in the PDDL problem provided below. You are free to modify the file as you see fit, but it must correctly model the described task. To aid you, you're provided with a checklist of common flaws you ought to address. Respond with the corrected PDDL problem file within a markdown block under the "# Revised Problem" header. The domain file is fixed and cannot be changed in any way. 

Checklist:
- Are there any syntactical mistakes?
- Do any actions have incorrect or missing effects?
- Do any actions have incorrect or missing preconditions?
- Is the type hierarchy correct?
- Are any predicates used inconsistently?
- Are there any predicates that should be used symmetrically but aren't? For example, "connected" or "neighbor" predicates should be `(or (connected ?a ?b) (connected ?b ?a))` rather than `(connected ?a ?b)`. 
- Are any predicates duplicates or opposites? For example, if "open" is a predicate, "closed" shouldn't be another predicate but instead inferred from "(not (open))".
- Are any actions overly similar and should be combined? If two actions function the same, but over different types, for example, they should likely be combined. It is possible that several different pairs of actions ought to be combined.
- Do any actions have implicit assumptions to be made explicit? There should be no need for assumptions, existing domain knowledge, or intuition when interpreting the actions.
- Are there any other flaws in the domain which must be addressed? This could be anything.

The domain aims to model the following:
> {domain_desc}

# Domain File
```
{domain}
```

# Problem File
```
{problem}
```
