# Core runtime dependencies for the default pipeline and evaluation flow.
# This set is intentionally lightweight so the repository is easy to reproduce.

openai>=1.66.0
retry>=0.9.2
addict>=2.4.0
tiktoken>=0.9.0
requests>=2.31.0
pddl>=0.4.0
